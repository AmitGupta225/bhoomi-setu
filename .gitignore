node_modules/
dist/
build/
.env
.env.*
!.env.example
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store

# Auth DB is auto-seeded on boot — don't commit password hashes
backend/data/auth.db

# Mock project/parcel/finance DBs ARE committed so Render has demo data on first boot

# Vite / React
frontend/node_modules/
frontend/dist/
frontend/.env
